/*todo CREATE CODE FOR EDIT BUTTON*/

/*const popupLink = document.querySelectorAll('.popup-link');
const body = document.querySelector('.body');

if (popupLink.length > 0) {
    for (let count = 0; count < popupLink.length; count++) {
        const popLink = popupLink[count];
        popLink.addEventListener('click', function (add){
            const popClass = popLink.getAttribute('href').replace('#', '');

        })
    }
}*/

